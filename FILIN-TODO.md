# FILIN-TODO — ветка `filin-main`

Цель: сделать ветку `filin-main` в форке `sovushik/openclaw`, которая даёт "из коробки" настройки под Филина, без секретов.

## Базовые задачи

1. **Агенты и биндинги по умолчанию**
   - Добавить в `openclaw.json` (или в стартовый конфиг по умолчанию):
     - агенты `filin`, `arina`, `ksenia` с workspace `/root/.openclaw/workspace`;
     - привязки Telegram:
       - `filin` ↔ Telegram id `1304329396`;
       - `arina` ↔ Telegram id `6321566145`;
       - `ksenia` ↔ Telegram id `544537329`;
     - `session.dmScope = "per-channel-peer"`.

2. **Модели и политики**
   - Для `filin` — зафиксировать дефолтную модель (по текущим договорённостям: `openai-codex/gpt-5.1`).
   - Для `arina` — по умолчанию `gpt-5.1`, переход на `gpt-5.2` только по явному запросу Филина; запрет `gpt-5.3-codex`.
   - Для `ksenia` — по умолчанию `gpt-5.1`, `gpt-5.2` только по запросу Филина.
   - Описать правила цветовых индикаторов (🔵/🟠/🟢) в системных файлах.

3. **Файлы агента по умолчанию**
   - Подложить шаблоны:
     - `AGENTS.md`
     - `SOUL.md`
     - `USER.md`
     - `TOOLS.md`
     - `MEMORY.md`
     - `IDENTITY.md`
   - Шаблоны должны быть без секретов, но с базовой структурой под Филина.

4. **Cron‑джобы "из коробки"**
   - Добавить cron‑job `healthcheck:update-status` с таймзоной `Asia/Yekaterinburg` и использованием скрипта `scripts/check_openclaw_update.sh`.

5. **Документация для filin-main**
   - Добавить краткий README/TODO по использованию ветки `filin-main` как базовой при установке (чем отличается от upstream `main`).

## Технические шаги (черновик)

- [x] Обновить локальный clone форка до актуального `upstream/main`.
- [x] Создать ветку `filin-main` от `upstream/main`.
- [ ] Внести изменения в дефолтный конфиг/шаблоны (пункты 1–4 выше).
- [ ] Обновить документацию в форке.
- [ ] Протестировать установку на тестовом окружении.
- [ ] Зафиксировать шаги установки из форка (отдельный README).

